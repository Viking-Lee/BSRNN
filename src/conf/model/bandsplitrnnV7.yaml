sr: 44100
n_fft: 2048
n_subbands: 41  # will generate 38 generic subbands, not 41
bottleneck_layer: "rnn"
t_timesteps: 259
fc_dim: 128
rnn_dim: 256
rnn_type: "LSTM"
bidirectional: True
num_layers: 12
mlp_dim: 512
return_mask: False

complex_as_channel: True
is_mono: ${..train_dataset.is_mono}
